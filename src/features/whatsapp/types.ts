export type {
  PublicConversation,
  PublicMessage,
  PublicMessageTemplate,
  ConversationCounts,
  ConversationFilters,
  ConversationQueue,
  ConversationStatus,
  MessageKind,
  MessageDirection,
  OriginKind,
} from '@shared/types';

import type { ConversationFilters } from '@shared/types';

export type UiFilters = ConversationFilters;
